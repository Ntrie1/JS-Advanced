function stringLength(a, b, c){
    const length = a.length + b.length + c.length;
    const average = Math.round(length / 3);
   
    console.log(length);
    console.log(average);





} stringLength('chocolate', 'ice cream', 'cake')