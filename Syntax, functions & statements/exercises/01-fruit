function fruit(typeOfFruit, weightOfFruit, price){

    let weightInKilos = weightOfFruit / 1000;
    let finalPrice = weightInKilos * price;
    console.log(`I need $${finalPrice.toFixed(2)} to buy ${weightInKilos.toFixed(2)} kilograms ${typeOfFruit}.`);





} fruit('orange', 2500, 1.80);
